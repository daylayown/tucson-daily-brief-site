__pycache__/
.venv/
transcripts/
public-record/.processed.txt
linkedin-post-*.md
agenda-watch/.scheduled.json
crossword/puzzles/.latest.txt
newsletter/drafts/
rag/index.sqlite
data/
in-depth/*-draft.md

# Records-request drafts + state — the repo is public; unsent requests stay local
records-requests/

# Promise ledger — machine-extracted rows are unreviewed; repo is public
promise-ledger/
EMAIL-SETUP-TDB.md

# Pre-publication investigation notebooks — the repo is public; working
# reporting notes, evidence, and data pulls stay local until a piece ships
rio-nuevo-investigation/
downtown-mass-shooting/
data-center-election-2026/
ranch-house-investigation/

# "How safe is Tucson?" In Depth — staged assets for an UNPUBLISHED piece that
# is gated on TPD's right-of-reply. Committing these would put the artwork, the
# evidence image, and the BTS carousel's framing of the FBI-reporting finding
# into a public repo before the subject of the story has had a chance to answer.
#
# ⛔ DELETE THIS BLOCK when the article publishes — the assets ship with it.
# Listed file-by-file on purpose: a glob (in-depth/*.png) would go on silently
# swallowing artwork for every future In Depth piece long after this one is out.
in-depth/SOCIAL-PLAN-how-safe-is-tucson.md
in-depth/evidence-me-homicides-pima-2026-07-20.jpg
in-depth/exec-3aa6ab2d-db3c-4751-86ab-3c05656a0796.png
in-depth/exec-5e0ba2c9-60aa-4f47-9c85-50cd3350f5ee.png
in-depth/fig-fbi-reporting-gap.svg
in-depth/hero-violence-datagap.svg
in-depth/hero-violence-what-we-know.png
in-depth/social-violence-desert-src.png
social/render_bts_datagap_carousel.py

# Around Town development-watch state
around-town/.dev_state.json
around-town/.dev_state_marana.json

# Spotted: Marana DLLC liquor-license snapshot (diff baseline)
public-record/.dllc_state_marana.json

# Design exploration mockups (local-only history, not for publishing)
design-explorations/

# Local strategy/design memos (private working notes)
kimi-analysis.md
kimi-operational-advice.md
DESIGN-DIRECTIONS-2026-07.md
IG-BTS-STRATEGY.md
sol-website-redesign.md

# Provenance-gate working dir: archived source text + shadow log
brief-inputs/

# Agenda attachment digests + diff manifests. Extracted text of public Legistar
# PDFs — regenerable, bulky (~58KB per meeting), and check_agendas.sh does a
# wholesale `git add agenda-watch/`, so without this they would auto-commit
# into the public repo daily. Legistar is the system of record.
agenda-watch/*-attachments.md
agenda-watch/*-attachments.json

# Model A/B: challenger briefs + metrics. Regenerable, never published.
brief-bake-off/

# PCAO dashboard captures — includes their internal metric definitions + SQL; do not republish
pcao-dashboard-archive/
